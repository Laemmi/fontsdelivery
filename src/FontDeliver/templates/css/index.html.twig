{% for item in list %}
@font-face {
    font-family   : '{{ item.getName()|raw }}';
    font-style    : {{ item.getCssStyle()|raw }};
    font-weight   : {{ item.getWeight()|raw }};
    src           : {{ item.getCssSrc()|raw }};
}
{% endfor %}